package main

import (
	"github.com/spf13/cobra"
)

// registerWindowsCommands registers all windows subcommands
func registerWindowsCommands(windowsCmd *cobra.Command) {
	// TODO: Add subcommands
	// windowsCmd.AddCommand(newConfigCmd())
	// windowsCmd.AddCommand(newServicesCmd())
	// etc.
}

