package main

import (
	"github.com/spf13/cobra"
)

// registerTeamsCommands registers all teams subcommands
func registerTeamsCommands(teamsCmd *cobra.Command) {
	// TODO: Add subcommands
	// teamsCmd.AddCommand(newConfigCmd())
	// teamsCmd.AddCommand(newListCmd())
	// etc.
}
